namespace DocxToPdf.Model {
	/// <summary>
	/// Represents a block-level element within a section or container (e.g. Paragraph, Table, Drawing).
	/// </summary>
	public interface IBlockElement {
	}
}
